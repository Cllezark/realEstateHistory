# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Generated data assets (produced by the pipeline publish stage).
# The files the app fetches at runtime ARE committed so the Railway deploy has
# data to serve - see the fetch calls in src/data/loaders.ts. Only these stale
# chunked-loading directories stay out; nothing reads them any more.
public/data/quarters/
public/data/sales/
public/data/tracts/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
